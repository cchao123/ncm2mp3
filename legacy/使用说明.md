# 网易云音乐 NCM 转 MP3 工具 - 使用说明

## ✅ 转换成功！

已成功将 NCM 文件转换为 MP3 格式并导出到 `output/` 目录！

### 转换结果

| 项目 | 信息 |
|------|------|
| 原文件 | `target/何宣林 - 冰天雪地.ncm` (9.2MB) |
| 转换后 | `output/何宣林 - 冰天雪地.mp3` (9.2MB) |
| 格式 | MPEG ADTS Layer III, 320 kbps, 48 kHz, 立体声 |
| 标签 | ID3 v2.4.0 |
| 状态 | ✅ 成功 |

---

## 📂 文件位置

转换后的 MP3 文件保存在：
```
output/何宣林 - 冰天雪地.mp3
```

---

## 🎵 播放测试

### 方式 1：使用系统默认播放器
```bash
open output/何宣林\ -\ 冰天雪地.mp3
```

### 方式 2：使用 VLC 播放器
```bash
vlc output/何宣林\ -\ 冰天雪地.mp3
```

### 方式 3：使用 ffplay
```bash
ffplay output/何宣林\ -\ 冰天雪地.mp3
```

---

## 🔄 批量转换使用

### 基本用法

```bash
./批量转换.sh <NCM文件或目录> [输出目录]
```

### 示例

```bash
# 转换单个文件（默认输出到 output/）
./批量转换.sh target/song.ncm

# 转换单个文件到指定目录
./批量转换.sh target/song.ncm my_music

# 批量转换目录（默认输出到 output/）
./批量转换.sh target

# 批量转换目录到指定位置
./批量转换.sh target ~/Music/converted
```

---

## 📝 脚本功能

- ✅ **自动创建输出目录**
- ✅ **批量处理多个文件**
- ✅ **转换成功统计**
- ✅ **可自定义输出路径**
- ✅ **保留完整 ID3 标签**

---

## 🎯 转换命令详解

脚本使用的核心命令：

```bash
ncmdump -o output <NCM文件或目录>
```

### ncmdump 参数说明

| 参数 | 说明 |
|------|------|
| `-o <目录>` | 指定输出目录 |
| `-f <格式>` | 自定义文件名格式 |
| `-d` | 转换后删除源文件 |
| `-c` | 覆盖同名文件 |
| `-r` | 自动重命名冲突文件 |

---

## ⚙️ 系统要求

### 必需组件

- Python 3.7+
- ncmdump 库

### 安装依赖

```bash
pip3 install ncmdump
```

### 验证安装

```bash
ncmdump -h
```

---

## 🔍 文件验证

转换后的 MP3 文件信息：

```bash
# 查看文件信息
file output/何宣林\ -\ 冰天雪地.mp3

# 查看详细信息（需安装 ffprobe）
ffprobe output/何宣林\ -\ 冰天雪地.mp3

# 查看文件头
xxd output/何宣林\ -\ 冰天雪地.mp3 | head -5
```

**验证结果：**
```
Audio file with ID3 version 2.4.0
MPEG ADTS, layer III, v1, 320 kbps, 48 kHz, Stereo
```

---

## 📚 技术说明

### NCM 格式解密流程

1. **文件头验证**：验证 `CTENFDAM` 魔术
2. **密钥解密**：AES-ECB + XOR 0x64
3. **密钥盒构建**：RC4-KSA 算法
4. **元数据解密**：AES-ECB + XOR 0x63
5. **音频解密**：RC4-PRGA 算法

### 文件结构

```
NCM 文件结构:
┌─────────────┬──────────────┬───────────┬────────┬─────────────┐
│ CTENFDAM    │ 加密密钥      │ 元数据     │ 图片    │ 加密音频     │
│ (8字节)     │ (AES+XOR)    │ (AES+XOR) │ (可选)   │ (RC4)       │
└─────────────┴──────────────┴───────────┴────────┴─────────────┘
```

---

## ⚠️ 注意事项

1. **版权声明**：本工具仅供个人学习和研究使用
2. **法律合规**：请遵守相关版权法律法规
3. **使用限制**：请勿用于商业用途
4. **文件用途**：转换后的文件仅供个人欣赏

---

## ❓ 常见问题

### Q: 转换后的文件可以正常播放吗？
A: 可以！转换后的文件是标准的 MP3 格式，包含完整的 ID3 标签信息，支持所有主流播放器。

### Q: 音质会损失吗？
A: 不会！转换过程是解密而非重新编码，完全保持原文件音质。

### Q: 为什么不使用自己实现的代码？
A: NCM 解密算法非常复杂，官方 ncmdump 库经过大量验证，更加可靠稳定。

### Q: 可以转换整个目录吗？
A: 可以！脚本支持批量处理目录中的所有 NCM 文件。

### Q: 如何自定义输出目录？
A:
```bash
# 方式 1：修改脚本中的 OUTPUT_DIR 变量
# 方式 2：运行时指定第二个参数
./批量转换.sh target my_output_dir
```

---

## 📖 参考资料

- **[nondanee/ncmdump](https://github.com/nondanee/ncmdump)** - Python 实现的 NCM 转换工具
- **[taurusxin/ncmdump](https://github.com/taurusxin/ncmdump)** - C++ 版本（更快）
- **[网易云音乐ncm格式分析](https://www.cnblogs.com/cyx-b/p/13443003.html)** - 详细技术分析

---

## 🎉 开始使用

现在你可以：

1. ✅ **播放音乐**：用任意播放器打开 `output/` 目录中的 MP3 文件
2. ✅ **批量转换**：将所有 NCM 文件放在一个目录中，使用批量转换脚本
3. ✅ **整理音乐**：转换后的文件可以复制到任意设备播放
4. ✅ **创建播放列表**：使用你喜欢的音乐管理器整理转换后的文件

---

**祝你使用愉快！** 🎵

---

**Sources:**
- [GitHub - taurusxin/ncmdump](https://github.com/taurusxin/ncmdump)
- [博客园 - 网易云音乐ncm格式分析](https://www.cnblogs.com/cyx-b/p/13443003.html)
- [GitHub - nondanee/ncmdump](https://github.com/nondanee/ncmdump)